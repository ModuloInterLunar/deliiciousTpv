<UserControl x:Class="Proyecto_Intermodular.userControls.OrderModel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Proyecto_Intermodular.userControls"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="400">
    <Border BorderBrush="AliceBlue" BorderThickness="1">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Label x:Name="lblDishName" Content="{Binding DishName}" VerticalContentAlignment="Center"/>
            <Label x:Name="lblEmployeeName" Content="{Binding EmployeeName}" Grid.Column="1" VerticalContentAlignment="Center"/>
            <CheckBox x:Name="ckBoxIsServed" Content="¿Servido?" IsChecked="True" Grid.Column="2" VerticalContentAlignment="Center"/>
        </Grid>
    </Border>
</UserControl>
